#include<stdio.h>
int main()
{
	int i,j,n,temp;

	printf("enter n:");
	scanf("%d",&n);
		int a[n];
	printf("numbers before swapping:\n");
	for(i=0;i<n;i++)
{	
    scanf("%d\n",&a[i]);
}
	for(i=0;i<n-1;i++)
	{
		for(j=i+1;j<n;j++)
		{
			
			
			if(a[j]<a[i]){
			
			temp=a[i];
	        a[i]=a[j];
         	a[j]=temp;
		}
	}
}
	
	printf("numbers after swapping:\n");
	for(i=0;i<n;i++)
{	
    printf("%d\n",a[i]);
	
	
}
return 0;


}
